---
title: Web
description: Korišćenje AlphaCode u vašem pretraživaču.
---

AlphaCode može raditi kao web aplikacija u vašem pretraživaču, pružajući isto moćno iskustvo AI kodiranja bez potrebe za terminalom.

![AlphaCode Web - Nova sesija](../../../assets/web/web-homepage-new-session.png)

## Početak rada

Pokrenite web interfejs tako što ćete pokrenuti:

```bash
alphacode web
```

Ovo pokreće lokalni server na `127.0.0.1` sa nasumičnim dostupnim portom i automatski otvara AlphaCode u vašem podrazumevanom pretraživaču.

:::caution
Ako `ALPHACODE_SERVER_PASSWORD` nije postavljen, server će biti nezaštićen. Ovo je u redu za lokalnu upotrebu, ali bi trebalo biti postavljeno za pristup mreži.
:::

:::tip[Windows korisnici]
Za najbolje iskustvo, pokrenite `alphacode web` iz [WSL](/docs/windows-wsl) umjesto PowerShell-a. Ovo osigurava pravilan pristup sistemu datoteka i integraciju terminala.
:::

---

## Konfiguracija

Možete konfigurirati web server koristeći oznake komandne linije ili u vašoj [config file](/docs/config).

### Port

AlphaCode podrazumevano bira dostupni port. Možete odrediti port:

```bash
alphacode web --port 4096
```

### Ime hosta

Podrazumevano, server se vezuje za `127.0.0.1` (samo lokalni host). Da biste AlphaCode učinili dostupnim na vašoj mreži:

```bash
alphacode web --hostname 0.0.0.0
```

Kada koristite `0.0.0.0`, AlphaCode će prikazati i lokalne i mrežne adrese:

```
  Local access:       http://localhost:4096
  Network access:     http://192.168.1.100:4096
```

### mDNS Otkrivanje

Omogućite mDNS kako bi vaš server bio vidljiv na lokalnoj mreži:

```bash
alphacode web --mdns
```

Ovo automatski postavlja ime hosta na `0.0.0.0` i oglašava server kao `alphacode.local`.

Možete prilagoditi ime mDNS domene za pokretanje više instanci na istoj mreži:

```bash
alphacode web --mdns --mdns-domain myproject.local
```

### CORS

Da biste omogućili dodatne domene za CORS (korisno za prilagođene frontendove):

```bash
alphacode web --cors https://example.com
```

### Autentifikacija

Da biste zaštitili pristup, postavite lozinku koristeći varijablu okruženja `ALPHACODE_SERVER_PASSWORD`:

```bash
ALPHACODE_SERVER_PASSWORD=secret alphacode web
```

Korisničko ime podrazumevano je `alphacode`, ali se može promeniti sa `ALPHACODE_SERVER_USERNAME`.

---

## Korištenje web sučelja

Jednom pokrenut, web sučelje pruža pristup vašim AlphaCode sesijama.

### Sesije

Pregledajte i upravljajte svojim sesijama sa početne stranice. Možete vidjeti aktivne sesije i započeti nove.

![AlphaCode Web - aktivna sesija](../../../assets/web/web-homepage-active-session.png)

### Status servera

Kliknite "Pogledajte servere" da vidite povezane servere i njihov status.

![AlphaCode Web - Vidi servere](../../../assets/web/web-homepage-see-servers.png)

---

## Povezivanje terminala

Možete priključiti TUI terminala na aktivni web server:

```bash
# Start the web server
alphacode web --port 4096

# In another terminal, attach the TUI
alphacode attach http://localhost:4096
```

Ovo vam omogućava da istovremeno koristite i web sučelje i terminal, dijeleći iste sesije i stanje.

---

## Konfiguracioni fajl

Također možete konfigurirati postavke servera u svom `alphacode.json` konfiguracijskom fajlu:

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "cors": ["https://example.com"]
  }
}
```

Oznake komandne linije imaju prednost nad postavkama konfiguracione datoteke.
