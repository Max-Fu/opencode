---
title: CLI
description: Opcje i polecenia wiersza poleceń AlphaCode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI AlphaCode domyślnie uruchamia [TUI](/docs/tui), gdy wywołasz je bez argumentów.

```bash
alphacode
```

CLI obsługuje też polecenia opisane na tej stronie, więc możesz używać AlphaCode programowo.

```bash
alphacode run "Explain how closures work in JavaScript"
```

---

### tui

Uruchamia interfejs terminalowy (TUI) AlphaCode.

```bash
alphacode [project]
```

#### Flagi

| Flaga                                    | Skrót | Opis                                                                    |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Kontynuuj ostatnią sesję                                                |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Identyfikator sesji do kontynuowania                                    |
| <nobr><code>{"--fork"}</code></nobr>     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Monit do użycia                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model do użycia w formacie dostawca/model                               |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent do użycia                                                         |
| <nobr><code>{"--port"}</code></nobr>     |       | Port do nasłuchiwania                                                   |
| <nobr><code>{"--hostname"}</code></nobr> |       | Nazwa hosta, do której należy się powiązać                              |

---

## Polecenia

Interfejs CLI AlphaCode zawiera także dodatkowe polecenia.

---

### agent

Zarządzaj agentami AlphaCode.

```bash
alphacode agent [command]
```

---

### attach

Dołącz terminal do już działającego serwera backendu AlphaCode uruchomionego za pomocą `serve` lub `web`.

```bash
alphacode attach [url]
```

Pozwala to połączyć TUI ze zdalnym backendem AlphaCode. Na przykład:

```bash
# Start the backend server for web/mobile access
alphacode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
alphacode attach http://10.20.30.40:4096
```

#### Flagi

| Flaga                                    | Skrót | Opis                                                                                                  |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Katalog roboczy, w którym uruchomić TUI                                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Kontynuuj ostatnią sesję                                                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Identyfikator sesji do kontynuowania                                                                  |
| <nobr><code>{"--fork"}</code></nobr>     |       | Rozgałęź sesję podczas kontynuowania (użyj z `--continue` lub `--session`)                            |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Hasło uwierzytelniania podstawowego (domyślnie `ALPHACODE_SERVER_PASSWORD`)                            |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nazwa użytkownika uwierzytelniania podstawowego (domyślnie `ALPHACODE_SERVER_USERNAME` lub `alphacode`) |

---

#### create

Utwórz nowego agenta z niestandardową konfiguracją.

```bash
alphacode agent create
```

To polecenie poprowadzi Cię przez proces tworzenia nowego agenta z niestandardowym promptem systemowym i konfiguracją narzędzi.

---

#### list

Wyświetl listę wszystkich agentów.

```bash
alphacode agent list
```

---

### auth

Zarządzaj danymi uwierzytelniającymi i logowaniem dla dostawców.

```bash
alphacode auth [command]
```

---

#### login

AlphaCode integruje się z wieloma dostawcami. Możesz użyć `alphacode auth login`, aby skonfigurować klucz API dla dowolnego obsługiwanego dostawcy. Klucze są bezpiecznie przechowywane w `~/.local/share/alphacode/auth.json`.

```bash
alphacode auth login
```

Podczas uruchamiania AlphaCode ładuje dostawców z pliku poświadczeń, a także ze zmiennych środowiskowych lub pliku `.env` w Twoim projekcie.

---

#### list

Wyświetl listę wszystkich uwierzytelnionych dostawców przechowywanych w pliku poświadczeń.

```bash
alphacode auth list
```

Lub skrócona wersja:

```bash
alphacode auth ls
```

---

#### logout

Wyloguj się z dostawcy, usuwając go z pliku poświadczeń.

```bash
alphacode auth logout
```

---

### github

Zarządzaj agentem GitHub w celu automatyzacji repozytorium.

```bash
alphacode github [command]
```

---

#### install

Zainstaluj agenta GitHub w swoim repozytorium.

```bash
alphacode github install
```

Spowoduje to utworzenie pliku workflow GitHub Actions i poprowadzi Cię przez proces konfiguracji. [Dowiedz się więcej](/docs/github).

---

#### run

Uruchom agenta na GitHubie. Zwykle używane w GitHub Actions.

```bash
alphacode github run
```

##### Flagi

| Flaga                                 | Opis                                     |
| ------------------------------------- | ---------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Zdarzenie GitHub, które wyzwoliło agenta |
| <nobr><code>{"--token"}</code></nobr> | Osobisty token dostępu GitHub            |

---

### mcp

Zarządzaj serwerami Model Context Protocol (MCP).

```bash
alphacode mcp [command]
```

---

#### add

Dodaj serwer MCP do swojej konfiguracji.

```bash
alphacode mcp add
```

To polecenie poprowadzi Cię przez proces dodawania lokalnego lub zdalnego serwera MCP.

---

#### list

Wyświetl listę wszystkich skonfigurowanych serwerów MCP i ich status.

```bash
alphacode mcp list
```

Lub skrócona wersja:

```bash
alphacode mcp ls
```

---

#### auth

Uwierzytelnij się w serwerze MCP z włączoną funkcją OAuth.

```bash
alphacode mcp auth [name]
```

Jeśli nie podasz nazwy serwera, zostaniesz poproszony o wybranie jednego z serwerów wymagających uwierzytelnienia.

Możesz także wyświetlić listę serwerów obsługujących OAuth i ich status uwierzytelnienia.

```bash
alphacode mcp auth list
```

Lub skrócona wersja:

```bash
alphacode mcp auth ls
```

---

#### logout

Usuń tokeny OAuth dla serwera MCP.

```bash
alphacode mcp logout [name]
```

---

#### debug

Debuguj problemy z połączeniem OAuth dla serwera MCP.

```bash
alphacode mcp debug <name>
```

---

### models

Wyświetl listę modeli ze wszystkich skonfigurowanych dostawców.

```bash
alphacode models [provider]
```

Pokazuje modele dostępne do użycia w `provider/model`.

Jest to identyfikator modelu, którego używasz w [konfiguracji](/docs/config).

Opcjonalnie możesz podać identyfikator dostawcy, aby filtrować modele według tego dostawcy.

```bash
alphacode models anthropic
```

#### Flagi

| Flaga                                   | Opis                                                                            |
| --------------------------------------- | ------------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Odśwież pamięć podręczną modeli                                                 |
| <nobr><code>{"--verbose"}</code></nobr> | Bardziej szczegółowe dane wyjściowe modelu (zawiera metadane, takie jak koszty) |

Użyj flagi `--refresh`, aby zaktualizować listę modeli w pamięci podręcznej. Jest to przydatne, gdy dostawca dodał nowe modele, które chcesz zobaczyć w AlphaCode.

```bash
alphacode models --refresh
```

---

### run

Uruchom AlphaCode w trybie nieinteraktywnym, przekazując bezpośrednio prompt.

```bash
alphacode run [message..]
```

Jest to przydatne do tworzenia skryptów, automatyzacji lub gdy chcesz wysłać szybkie zapytanie bez uruchamiania TUI. Na przykład:

```bash "alphacode run"
alphacode run Explain the use of context in Go
```

Można również użyć tego z działającym `alphacode serve`, aby uniknąć zimnego startu serwera MCP przy każdym uruchomieniu:

```bash
# Start a headless server in one terminal
alphacode serve

# In another terminal, run commands that attach to it
alphacode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagi

| Flaga                                    | Skrót | Opis                                                                                                  |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Polecenie do uruchomienia, reszta to argumenty                                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Kontynuuj ostatnią sesję                                                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Identyfikator sesji do kontynuowania                                                                  |
| <nobr><code>{"--fork"}</code></nobr>     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`)                               |
| <nobr><code>{"--share"}</code></nobr>    |       | Udostępnij sesję po zakończeniu                                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model do użycia w formacie dostawca/model                                                             |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent do użycia                                                                                       |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | Pliki do załączenia do wiadomości                                                                     |
| <nobr><code>{"--format"}</code></nobr>   |       | Format wyjściowy: `default` (sformatowany) lub `json` (surowy JSON)                                   |
| <nobr><code>{"--title"}</code></nobr>    |       | Tytuł sesji (jeśli nie podano, zostanie wygenerowany z promptu)                                       |
| <nobr><code>{"--attach"}</code></nobr>   |       | Dołącz do działającego serwera AlphaCode (np. http://localhost:4096)                                   |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Hasło uwierzytelniania podstawowego (domyślnie `ALPHACODE_SERVER_PASSWORD`)                            |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nazwa użytkownika uwierzytelniania podstawowego (domyślnie `ALPHACODE_SERVER_USERNAME` lub `alphacode`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Katalog do uruchomienia lub ścieżka na zdalnym serwerze podczas dołączania                            |
| <nobr><code>{"--variant"}</code></nobr>  |       | Wariant modelu (poziom wnioskowania specyficzny dla dostawcy)                                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Pokaż bloki myślenia                                                                                  |
| <nobr><code>{"--port"}</code></nobr>     |       | Port dla serwera lokalnego (domyślnie losowy)                                                         |

---

### serve

Uruchom serwer AlphaCode (bez interfejsu) w celu uzyskania dostępu do API. Pełny opis API HTTP znajduje się w [dokumentacji serwera](/docs/server).

```bash
alphacode serve
```

Uruchamia to serwer HTTP, który zapewnia dostęp do API AlphaCode bez interfejsu TUI. Ustaw `ALPHACODE_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `alphacode`).

#### Flagi

| Flaga                                    | Opis                                       |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Port do nasłuchiwania                      |
| <nobr><code>{"--hostname"}</code></nobr> | Nazwa hosta, do której należy się powiązać |
| <nobr><code>{"--mdns"}</code></nobr>     | Włącz wykrywanie mDNS                      |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatkowe dozwolone źródła CORS            |

---

### session

Zarządzaj sesjami AlphaCode.

```bash
alphacode session [command]
```

---

#### list

Lista wszystkich sesji AlphaCode.

```bash
alphacode session list
```

##### Flagi

| Flaga                                     | Skrót | Opis                                       |
| ----------------------------------------- | ----- | ------------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Ogranicz do ostatnich N sesji              |
| <nobr><code>{"--format"}</code></nobr>    |       | Format wyjściowy: tabela lub json (tabela) |

---

### stats

Pokaż statystyki wykorzystania tokenów i sesji AlphaCode.

```bash
alphacode stats
```

#### Flagi

| Flaga                                   | Opis                                                                                  |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Pokaż statystyki z ostatnich N dni (domyślnie: cały czas)                             |
| <nobr><code>{"--tools"}</code></nobr>   | Pokaż użycie poszczególnych narzędzi (domyślnie: wszystkie)                           |
| <nobr><code>{"--models"}</code></nobr>  | Pokaż podział na modele (domyślnie ukryty). Podaj liczbę, aby pokazać N najczęstszych |
| <nobr><code>{"--project"}</code></nobr> | Filtruj według projektu (domyślnie: wszystkie projekty, pusty ciąg: bieżący projekt)  |

---

### export

Eksportuj dane sesji jako JSON.

```bash
alphacode export [sessionID]
```

Jeśli nie podasz identyfikatora sesji, zostaniesz poproszony o wybranie jednej z ostatnich sesji.

---

### import

Importuj dane sesji z pliku JSON lub adresu URL udostępniania AlphaCode.

```bash
alphacode import <file>
```

Możesz importować z pliku lokalnego lub adresu URL.

```bash
alphacode import session.json
alphacode import https://opncd.ai/s/abc123
```

---

### web

Uruchom serwer AlphaCode z interfejsem internetowym.

```bash
alphacode web
```

Uruchamia to serwer HTTP i udostępnia AlphaCode przez interfejs przeglądarkowy. Ustaw `ALPHACODE_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `alphacode`).

#### Flagi

| Flaga                                    | Opis                                       |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Port do nasłuchiwania                      |
| <nobr><code>{"--hostname"}</code></nobr> | Nazwa hosta, do której należy się powiązać |
| <nobr><code>{"--mdns"}</code></nobr>     | Włącz wykrywanie mDNS                      |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatkowe dozwolone źródła CORS            |

---

### acp

Uruchom serwer ACP (Agent Client Protocol).

```bash
alphacode acp
```

Uruchamia serwer ACP, który komunikuje się przez stdin/stdout przy użyciu JSON-RPC.

#### Flagi

| Flaga                                    | Opis                                       |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>      | Katalog roboczy                            |
| <nobr><code>{"--port"}</code></nobr>     | Port do nasłuchiwania                      |
| <nobr><code>{"--hostname"}</code></nobr> | Nazwa hosta, do której należy się powiązać |

---

### uninstall

Odinstaluj AlphaCode i usuń wszystkie powiązane pliki.

```bash
alphacode uninstall
```

#### Flagi

| Flaga                                       | Skrót | Opis                          |
| ------------------------------------------- | ----- | ----------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Zachowaj pliki konfiguracyjne |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Zachowaj dane sesji i migawki |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Pokaż co zostanie usunięte    |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Pomiń monity o potwierdzenie  |

---

### upgrade

Aktualizuj AlphaCode do najnowszej lub określonej wersji.

```bash
alphacode upgrade [target]
```

Aby zaktualizować do najnowszej wersji:

```bash
alphacode upgrade
```

Aby zaktualizować do konkretnej wersji:

```bash
alphacode upgrade v0.1.48
```

#### Flagi

| Flaga                                  | Skrót | Opis                                                |
| -------------------------------------- | ----- | --------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Wymuś metodę instalacji: curl, npm, pnpm, bun, brew |

---

## Flagi globalne

Interfejs CLI AlphaCode przyjmuje następujące flagi globalne dla każdego polecenia.

| Flaga                                      | Skrót | Opis                                        |
| ------------------------------------------ | ----- | ------------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Wyświetl pomoc                              |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Wydrukuj numer wersji                       |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Drukuj logi na stderr                       |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Poziom logowania (DEBUG, INFO, WARN, ERROR) |

---

## Zmienne środowiskowe

AlphaCode można skonfigurować za pomocą zmiennych środowiskowych.

| Zmienna                               | Typ     | Opis                                                       |
| ------------------------------------- | ------- | ---------------------------------------------------------- |
| `ALPHACODE_AUTO_SHARE`                 | boolean | Automatycznie udostępniaj sesje                            |
| `ALPHACODE_GIT_BASH_PATH`              | string  | Ścieżka do pliku wykonywalnego Git Bash w systemie Windows |
| `ALPHACODE_CONFIG`                     | string  | Ścieżka do pliku konfiguracyjnego                          |
| `ALPHACODE_TUI_CONFIG`                 | string  | Ścieżka do pliku konfiguracyjnego TUI                      |
| `ALPHACODE_CONFIG_DIR`                 | string  | Ścieżka do katalogu konfiguracyjnego                       |
| `ALPHACODE_CONFIG_CONTENT`             | string  | Treść konfiguracji JSON (inline)                           |
| `ALPHACODE_DISABLE_AUTOUPDATE`         | boolean | Wyłącz automatyczne sprawdzanie aktualizacji               |
| `ALPHACODE_DISABLE_PRUNE`              | boolean | Wyłącz czyszczenie starych wyników (pruning)               |
| `ALPHACODE_DISABLE_TERMINAL_TITLE`     | boolean | Wyłącz automatyczne ustawianie tytułu terminala            |
| `ALPHACODE_PERMISSION`                 | string  | Konfiguracja uprawnień w JSON (inline)                     |
| `ALPHACODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Wyłącz domyślne wtyczki                                    |
| `ALPHACODE_DISABLE_LSP_DOWNLOAD`       | boolean | Wyłącz automatyczne pobieranie serwerów LSP                |
| `ALPHACODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Włącz modele eksperymentalne                               |
| `ALPHACODE_DISABLE_AUTOCOMPACT`        | boolean | Wyłącz automatyczne kompaktowanie kontekstu                |
| `ALPHACODE_DISABLE_CLAUDE_CODE`        | boolean | Wyłącz integrację z `.claude` (prompt + skills)            |
| `ALPHACODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Wyłącz czytanie `~/.claude/CLAUDE.md`                      |
| `ALPHACODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Wyłącz ładowanie `.claude/skills`                          |
| `ALPHACODE_DISABLE_MODELS_FETCH`       | boolean | Wyłącz pobieranie modeli ze źródeł zewnętrznych            |
| `ALPHACODE_FAKE_VCS`                   | string  | Fałszywy dostawca VCS do celów testowych                   |
| `ALPHACODE_CLIENT`                     | string  | Identyfikator klienta (domyślnie `cli`)                    |
| `ALPHACODE_ENABLE_EXA`                 | boolean | Włącz narzędzie wyszukiwania internetowego Exa             |
| `ALPHACODE_SERVER_PASSWORD`            | string  | Włącz uwierzytelnianie podstawowe dla `serve`/`web`        |
| `ALPHACODE_SERVER_USERNAME`            | string  | Nazwa użytkownika do autoryzacji (domyślnie `alphacode`)    |
| `ALPHACODE_MODELS_URL`                 | string  | Niestandardowy adres URL do pobierania konfiguracji modeli |

---

### Eksperymentalne

Te zmienne włączają funkcje eksperymentalne, które mogą ulec zmianie lub zostać usunięte.

| Zmienna                                         | Typ     | Opis                                                |
| ----------------------------------------------- | ------- | --------------------------------------------------- |
| `ALPHACODE_EXPERIMENTAL`                         | boolean | Włącz funkcje eksperymentalne objęte flagą zbiorczą |
| `ALPHACODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Włącz wykrywanie ikon                               |
| `ALPHACODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Wyłącz kopiowanie przy zaznaczaniu w TUI            |
| `ALPHACODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Domyślny limit czasu dla narzędzia bash w ms        |
| `ALPHACODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksymalne tokeny wyjściowe dla LLM                 |
| `ALPHACODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Włącz obserwatora plików dla całego katalogu        |
| `ALPHACODE_EXPERIMENTAL_OXFMT`                   | boolean | Włącz formater oxfmt                                |
| `ALPHACODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Włącz eksperymentalne narzędzie LSP                 |
| `ALPHACODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Wyłącz obserwatora plików                           |
| `ALPHACODE_EXPERIMENTAL_EXA`                     | boolean | Włącz funkcje eksperymentalne Exa                   |
| `ALPHACODE_EXPERIMENTAL_LSP_TY`                  | boolean | Włącz TY LSP dla plików python                      |
| `ALPHACODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Włącz tryb planowania                               |
| `ALPHACODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Włącz zadania subagentów w tle                      |
| `ALPHACODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Włącz eksperymentalny system zdarzeń                |
| `ALPHACODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Włącz natywną ścieżkę żądań LLM                     |
| `ALPHACODE_EXPERIMENTAL_PARALLEL`                | boolean | Włącz równoległe wykonywanie wyszukiwania web       |
| `ALPHACODE_EXPERIMENTAL_SCOUT`                   | boolean | Włącz subagenta Scout                               |
| `ALPHACODE_EXPERIMENTAL_WORKSPACES`              | boolean | Włącz obsługę obszarów roboczych                    |
