---
title: CLI
description: AlphaCode CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The AlphaCode CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
alphacode
```

But it also accepts commands as documented on this page. This allows you to interact with AlphaCode programmatically.

```bash
alphacode run "Explain how closures work in JavaScript"
```

---

### tui

Start the AlphaCode terminal user interface.

```bash
alphacode [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--auto"}</code></nobr>        |       | Auto-approve permissions that are not explicitly denied                 |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The AlphaCode CLI also has the following commands.

---

### agent

Manage agents for AlphaCode.

```bash
alphacode agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
alphacode agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.alphacode/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
alphacode agent list
```

---

### attach

Attach a terminal to an already running AlphaCode backend server started via `serve` or `web` commands.

```bash
alphacode attach [url]
```

This allows using the TUI with a remote AlphaCode backend. For example:

```bash
# Start the backend server for web/mobile access
alphacode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
alphacode attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `ALPHACODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `ALPHACODE_SERVER_USERNAME` or `alphacode`) |

---

### auth

Command to manage credentials and login for providers.

```bash
alphacode auth [command]
```

---

#### login

AlphaCode is powered by the provider list at [Models.dev](https://models.dev), so you can use `alphacode auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/alphacode/auth.json`.

```bash
alphacode auth login
```

When AlphaCode starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
alphacode auth list
```

Or the short version.

```bash
alphacode auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
alphacode auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
alphacode github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
alphacode github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
alphacode github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
alphacode mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
alphacode mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
alphacode mcp list
```

Or use the short version.

```bash
alphacode mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
alphacode mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
alphacode mcp auth list
```

Or use the short version.

```bash
alphacode mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
alphacode mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
alphacode mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
alphacode models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
alphacode models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in AlphaCode.

```bash
alphacode models --refresh
```

---

### run

Run alphacode in non-interactive mode by passing a prompt directly.

```bash
alphacode run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "alphacode run"
alphacode run Explain the use of context in Go
```

You can also attach to a running `alphacode serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
alphacode serve

# In another terminal, run commands that attach to it
alphacode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>    |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>   |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>    |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach to a running alphacode server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `ALPHACODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `ALPHACODE_SERVER_USERNAME` or `alphacode`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>     |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>  |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Show thinking blocks                                                       |
| <nobr><code>{"--auto"}</code></nobr>     |       | Auto-approve permissions that are not explicitly denied                    |

---

### serve

Start a headless AlphaCode server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
alphacode serve
```

This starts an HTTP server that provides API access to alphacode functionality without the TUI interface. Set `ALPHACODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `alphacode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage AlphaCode sessions.

```bash
alphacode session [command]
```

---

#### list

List all AlphaCode sessions.

```bash
alphacode session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an AlphaCode session.

```bash
alphacode session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your AlphaCode sessions.

```bash
alphacode stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
alphacode export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or AlphaCode share URL.

```bash
alphacode import <file>
```

You can import from a local file or an AlphaCode share URL.

```bash
alphacode import session.json
alphacode import https://opncd.ai/s/abc123
```

---

### web

Start a headless AlphaCode server with a web interface.

```bash
alphacode web
```

This starts an HTTP server and opens a web browser to access AlphaCode through a web interface. Set `ALPHACODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `alphacode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
alphacode acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
alphacode plugin <module>
```

Or use the alias.

```bash
alphacode plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run AlphaCode.

```bash
alphacode pr <number>
```

---

### db

Database tools.

```bash
alphacode db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
alphacode db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
alphacode debug [command]
```

---

### uninstall

Uninstall AlphaCode and remove all related files.

```bash
alphacode uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates alphacode to the latest version or a specific version.

```bash
alphacode upgrade [target]
```

To upgrade to the latest version.

```bash
alphacode upgrade
```

To upgrade to a specific version.

```bash
alphacode upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The alphacode CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

AlphaCode can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `ALPHACODE_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `ALPHACODE_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `ALPHACODE_CONFIG`                     | string  | Path to config file                               |
| `ALPHACODE_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `ALPHACODE_CONFIG_DIR`                 | string  | Path to config directory                          |
| `ALPHACODE_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `ALPHACODE_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `ALPHACODE_ENABLE_AUTOUPDATE`          | boolean | Enable update checks (off by default)             |
| `ALPHACODE_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `ALPHACODE_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `ALPHACODE_PERMISSION`                 | string  | Inlined json permissions config                   |
| `ALPHACODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `ALPHACODE_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `ALPHACODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `ALPHACODE_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `ALPHACODE_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `ALPHACODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `ALPHACODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `ALPHACODE_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `ALPHACODE_ENABLE_MODELS_FETCH`        | boolean | Fetch the model catalog remotely (off by default) |
| `ALPHACODE_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `ALPHACODE_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `ALPHACODE_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `ALPHACODE_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `ALPHACODE_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `ALPHACODE_SERVER_USERNAME`            | string  | Override basic auth username (default `alphacode`) |
| `ALPHACODE_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `ALPHACODE_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `ALPHACODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `ALPHACODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `ALPHACODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `ALPHACODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `ALPHACODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `ALPHACODE_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `ALPHACODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `ALPHACODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `ALPHACODE_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `ALPHACODE_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `ALPHACODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `ALPHACODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `ALPHACODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `ALPHACODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `ALPHACODE_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `ALPHACODE_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `ALPHACODE_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
